<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="24dp"
    android:background="@drawable/bg_header_home">

    <TextView
        android:id="@+id/home_header_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="?attr/colorSecondary"
        android:text="@string/header_finduser"
        android:textStyle="bold"
        android:textSize="24sp"
        android:padding="16dp"
        android:layout_marginTop="32dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <androidx.appcompat.widget.SearchView
        android:id="@+id/search_home_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_search_field"
        app:searchIcon="@drawable/ic_at"
        app:layout_constraintStart_toStartOf="@id/home_header_tv"
        app:layout_constraintEnd_toEndOf="@id/home_header_tv"
        app:layout_constraintTop_toBottomOf="@id/home_header_tv"/>


</androidx.constraintlayout.widget.ConstraintLayout>